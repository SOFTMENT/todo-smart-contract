# Foundry build
/out/
/cache/

# Broadcast: keep folder; ignore local & dry-runs; keep only latest per network
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/
/broadcast/**/run-*.json
!/broadcast/**/run-latest.json

# Env
.env

# OS/editor noise (optional)
.DS_Store